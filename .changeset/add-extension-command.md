---
'vercel': minor
---

Add `vercel extension` command for managing CLI extensions. Supports `install` (from GitHub or local path), `list`, and `remove` subcommands. Extensions are installed to `~/.vercel/extensions/` and discovered automatically when invoking `vercel <extension-name>`.
